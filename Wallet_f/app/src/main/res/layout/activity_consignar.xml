<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/consignar"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#F5F2F8">

    <!-- Barra de Título Superior -->
    <TextView
        android:id="@+id/tvTitulo3"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="40dp"
        android:text="Recibir Dinero"
        android:textColor="#1B1B1B"
        android:textSize="22sp"
        android:textStyle="bold"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- Asegúrate de tener este ícono -->

    <!-- Contenedor Principal Centrado -->

    <androidx.cardview.widget.CardView
        android:id="@+id/qr_card"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="24dp"
        android:layout_marginEnd="24dp"
        app:cardBackgroundColor="@color/white"
        app:cardCornerRadius="28dp"
        app:cardElevation="4dp"
        app:layout_constraintBottom_toTopOf="@id/btn_listo"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent">

    </androidx.cardview.widget.CardView>


    <!-- Botón Inferior -->

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btn_listo"
        android:layout_width="323dp"
        android:layout_height="44dp"
        android:layout_marginBottom="32dp"
        android:backgroundTint="#6750A4"
        android:text="Listo"
        android:textColor="@color/white"
        app:cornerRadius="28dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="24dp"
        app:layout_constraintBottom_toTopOf="@+id/btn_listo"
        app:layout_constraintTop_toBottomOf="@+id/tvTitulo3"
        tools:layout_editor_absoluteX="0dp">

        <!-- Imagen para el Código QR -->
        <!-- Placeholder para la vista previa -->

        <!-- Texto de Instrucción -->
        <ImageView
            android:id="@+id/qr_user"
            android:layout_width="250dp"
            android:layout_height="250dp"
            android:background="#E0E0E0"
            android:contentDescription="Código QR para recibir dinero"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintHorizontal_bias="0.495"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:srcCompat="@tools:sample/avatars" />

        <TextView
            android:id="@+id/tv_instruction"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="24dp"
            android:gravity="center"
            android:text="Muestra este código para que te envíen dinero o comparte tu número."
            android:textColor="#49454F"
            android:textSize="16sp"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/qr_user" />

        <!-- Contenedor para el Número y el Botón de Copiar -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:gravity="center"
            android:orientation="horizontal"
            android:padding="12dp"
            app:layout_constraintTop_toBottomOf="@id/tv_instruction">

            <TextView
                android:id="@+id/tv_user_tel"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:textColor="#1D1B20"
                android:textSize="18sp"
                android:textStyle="bold"
                tools:text="300 123 4567" />

            <ImageButton
                android:id="@+id/btn_copy_number"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:src="@android:drawable/ic_menu_save"
                android:contentDescription="Copiar número"
                app:tint="#6750A4" />


        </LinearLayout>

    </androidx.constraintlayout.widget.ConstraintLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
